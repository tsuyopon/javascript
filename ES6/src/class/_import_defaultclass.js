// 1つのモジュールをexportするときにexport defaultが推奨されている
export default class Base {
  constructor(name) {
    this.name = name;
  }
  showName() {
    console.log(`my name is ${this.name}`);
  }
}
 
    super(name);
  }
}

